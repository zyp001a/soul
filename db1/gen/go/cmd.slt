~
goImport("os/exec")
goImport("strings")
@if(!#1){
 #in = '""'
}@else{
 #in = exec(#1, $env)
}
~func (x string) string {cmd := exec.Command("/bin/bash", "-c", &0); if(x != ""){cmd.Stdin = strings.NewReader(x)}; out, err := cmd.Output(); _=err; return string(out); }(~=in~)